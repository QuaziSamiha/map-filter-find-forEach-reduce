const numbers = [2, 5, 7, 6, 8];

// for(let i = 0; i < numbers.length; i++){
//     const element = numbers[i];
//     console.log(element);
// }

numbers.map(function(element){ // anonymous function
    console.log(element);
});